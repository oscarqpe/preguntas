module.exports = function(app) {
  var User = app.models.User;

  /*
  User.create([
    {id: 1, username: 'admin', email: 'admin@admin.com', password: '1'}
  ], function(err, users) {
  	console.log(err);
  	console.log(users);
    if (err) throw err;

    console.log('Created users:', users);
  });
*/
};